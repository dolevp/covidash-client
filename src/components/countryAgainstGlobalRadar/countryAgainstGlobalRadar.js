export default function CountryAgainstGlobalRadar({ country, statistics }) {
  const countryStats = {
  }
  const globalStats = {
  }
  if (countryStats === globalStats) {
    // TODO: same data text
    return null
  }
  return null
}
